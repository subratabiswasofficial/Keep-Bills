const loginRouter = require('./login');
const studentRouter = require('./student');
const billRouter = require('./bill');
const adminRouter = require('./admin');

module.exports = { loginRouter, studentRouter, billRouter, adminRouter };
