const authController = require('./login');
const studentController = require('./student');
const billController = require('./bill');
const adminController = require('./admin');

module.exports = { authController, studentController, billController, adminController };
